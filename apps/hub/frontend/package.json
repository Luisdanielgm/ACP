{
  "private": true,
  "name": "acp-hub-frontend",
  "workspaces": [
    "packages/shared",
    "packages/public-app",
    "packages/managed-app"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/public-app && npm run build --workspace=packages/managed-app",
    "dev:public": "npm run dev --workspace=packages/public-app",
    "dev:managed": "npm run dev --workspace=packages/managed-app"
  }
}
